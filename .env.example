# Supabase configuration (shared with rcs-report-card-tool)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Note: These are public credentials (VITE_ prefix means they're exposed in the browser)
# They only have read access to the courses RPC and should not expose sensitive data
